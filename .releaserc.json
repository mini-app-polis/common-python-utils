{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/exec",
      {
        "verifyConditionsCmd": "./scripts/verify-pypi-publishing.sh",
        "prepareCmd": "printf '__version__ = \"%s\"\\n' \"${nextRelease.version}\" > src/mini_app_polis/_version.py",
        "publishCmd": "uv build && uv publish --trusted-publishing always"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "src/mini_app_polis/_version.py"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    "@semantic-release/github"
  ]
}
